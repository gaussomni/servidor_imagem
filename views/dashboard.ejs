<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Gerenciador de Imagens</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <header>
    <div class="header-container">
      <h1><i class="fas fa-images"></i> Gerenciador de Imagens</h1>
    </div>
  </header>

  <div class="container">
    <!-- Breadcrumbs -->
    <div class="breadcrumb">
      <i class="fas fa-home"></i>
      <span>Home</span>
    </div>

    <!-- Alertas (serão exibidos dinamicamente) -->
    <div id="alert-container"></div>

    <!-- Criar Pasta -->
    <div class="card">
      <div class="card-header">
        <h2 class="card-title"><i class="fas fa-folder-plus"></i> Criar Nova Pasta</h2>
      </div>
      <form action="/pasta" method="POST" id="criar-pasta-form">
        <input type="hidden" name="caminhoAtual" value="<%= caminhoAtual %>">
        <div class="flex gap-2">
          <input type="text" name="nomePasta" placeholder="Nome da pasta" required>
          <button type="submit"><i class="fas fa-plus"></i> Criar Pasta</button>
        </div>
      </form>
    </div>

    <!-- Pastas -->
    <div class="card">
      <div class="card-header">
        <h2 class="card-title"><i class="fas fa-folder"></i> Pastas</h2>
      </div>
      
      <% if (pastas.length === 0) { %>
        <div class="text-center mt-4 mb-4">
          <p><i class="fas fa-info-circle"></i> Nenhuma pasta encontrada. Crie uma nova pasta acima.</p>
        </div>
      <% } else { %>
        <div class="folder-grid">
          <% pastas.forEach(pasta => { %>
            <div class="folder-item">
              <div class="folder-icon">
                <i class="fas fa-folder"></i>
              </div>
              <div class="folder-name"><%= pasta %></div>
              <div class="folder-actions">
                <a href="/browse?path=<%= encodeURIComponent(pasta) %>" class="tooltip">
                  <button><i class="fas fa-eye"></i></button>
                  <span class="tooltip-text">Abrir</span>
                </a>
                <form action="/pasta" method="POST" class="delete-form">
                  <input type="hidden" name="_method" value="DELETE">
                  <input type="hidden" name="caminhoAtual" value="<%= caminhoAtual %>">
                  <input type="hidden" name="pasta" value="<%= pasta %>">
                  <button type="submit" class="danger tooltip delete-pasta-btn">
                    <i class="fas fa-trash"></i>
                    <span class="tooltip-text">Excluir</span>
                  </button>
                </form>
              </div>
            </div>
          <% }); %>
        </div>
      <% } %>
    </div>
  </div>

  <!-- Confirmar exclusão modal -->
  <div id="confirmar-exclusao-modal" class="modal">
    <div class="modal-content">
      <span class="fechar" onclick="fecharModal()">&times;</span>
      <div class="modal-header">
        <h3 class="modal-title"><i class="fas fa-exclamation-triangle"></i> Confirmar Exclusão</h3>
      </div>
      <p>Tem certeza que deseja excluir esta pasta? Esta ação não pode ser desfeita e todos os arquivos dentro da pasta serão excluídos permanentemente.</p>
      <div class="flex justify-between mt-4">
        <button class="secondary" onclick="fecharModal()">Cancelar</button>
        <button id="confirmar-exclusao-btn" class="danger">Sim, excluir permanentemente</button>
      </div>
    </div>
  </div>

  <script>
    // Função para mostrar alertas
    function mostrarAlerta(mensagem, tipo = 'info') {
      const container = document.getElementById('alert-container');
      const alerta = document.createElement('div');
      alerta.className = `alert alert-${tipo}`;
      alerta.innerHTML = mensagem;
      
      container.appendChild(alerta);
      
      // Remover alerta após 5 segundos
      setTimeout(() => {
        alerta.style.opacity = '0';
        setTimeout(() => {
          container.removeChild(alerta);
        }, 300);
      }, 5000);
    }
    
    // Interceptar formulário de criação de pasta
    document.getElementById('criar-pasta-form').addEventListener('submit', async function(e) {
      e.preventDefault();
      
      const form = e.target;
      const formData = new FormData(form);
      
      try {
        const response = await fetch(form.action, {
          method: 'POST',
          body: formData
        });
        
        if (response.ok) {
          window.location.reload();
        } else {
          const error = await response.text();
          mostrarAlerta(error, 'danger');
        }
      } catch (err) {
        mostrarAlerta('Erro ao criar pasta: ' + err.message, 'danger');
      }
    });
    
    // Confirmar exclusão de pasta
    let formAtual = null;
    
    document.querySelectorAll('.delete-pasta-btn').forEach(btn => {
      btn.addEventListener('click', function(e) {
        e.preventDefault();
        formAtual = this.closest('form');
        document.getElementById('confirmar-exclusao-modal').style.display = 'block';
      });
    });
    
    document.getElementById('confirmar-exclusao-btn').addEventListener('click', function() {
      if (formAtual) {
        formAtual.submit();
      }
      fecharModal();
    });
    
    function fecharModal() {
      document.getElementById('confirmar-exclusao-modal').style.display = 'none';
    }
    
    // Fechar modal ao clicar fora
    window.onclick = function(event) {
      const modal = document.getElementById('confirmar-exclusao-modal');
      if (event.target == modal) {
        modal.style.display = 'none';
      }
    }
  </script>
</body>
</html>